Run the Factory agent — website build tracker and AIW template manager.

Usage: /factory /new-build [client] [niche] [tier] | /stage-update [build_id] [stage] | /build-status [build_id] | /complete [build_id] [url]

```bash
claude -p "$(cat agency-os/agents/04-factory.md)" --print "$ARGUMENTS"
```

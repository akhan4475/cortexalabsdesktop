Run the Setter agent — follow-up sequences and Cal.com booking sync.

Usage: /setter /followup | /followup [lead_id] | /check-bookings | /overdue

```bash
claude -p "$(cat agency-os/agents/02-setter.md)" --print "$ARGUMENTS"
```

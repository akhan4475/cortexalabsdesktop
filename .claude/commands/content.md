Run the Content agent — personal brand scripts, hooks, batches.

Usage: /content /script [topic] [format] [niche] | /batch [count] | /hook [topic] | /approve [script_id]

```bash
claude -p "$(cat agency-os/agents/06-content.md)" --print "$ARGUMENTS"
```

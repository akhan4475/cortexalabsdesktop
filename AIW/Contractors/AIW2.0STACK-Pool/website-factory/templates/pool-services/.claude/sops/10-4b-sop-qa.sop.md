# SOP 10.4b - SOP QA

Implements: Stage 10.4b.

## Purpose

Run the universal SOP compliance QA pass and the pool-services-specific checklist. Gate: all HALT items must pass before deploy.

## Inputs (per-client)

```
clients/[Client Name]/[Client Name] Website/dist/
niche-playbook/cro-rules.md
niche-playbook/copy-blocklist-additions.md
.claude/checklists/sop-compliance.md
```

## Procedure

1. Run sop-qa-agent.
2. Load `.claude/checklists/sop-compliance.md`. Run every HALT check.
3. Load `niche-playbook/sop-overrides/00-master.md` Stage 10.4b section. Run niche-specific checks.
4. Load `niche-playbook/copy-blocklist-additions.md`. Search built HTML for banned phrases.
5. Search built HTML for em-dashes (character: `—`). Any found is a HALT.
6. Search built HTML for any surviving `__REQUIRED__` sentinels. Any found is a HALT.
7. Verify `heroTrustChips` renders minimum 3 chips on homepage.
8. Verify H1 on homepage contains client's city name.
9. Verify phone number has `href="tel:` attribute.
10. Score pass/fail per check. Total HALT count must be 0 to proceed.

## Pass criteria

- 0 HALT items.
- WARN items logged but do not block.

## When this stage halts

- Any HALT item fails: document the failure, fix the root cause, re-run the full checklist.
- Do not override a HALT item without the student's explicit approval via `/override-sop`.

## Niche-specific notes

Pool-services HALT checks (from `sop-overrides/00-master.md`):
- `heroTrustChips.length >= 3`
- H1 on homepage contains city name
- Phone is `tel:` link in nav
- No em-dashes in any built HTML
- `copy.privacyLine` is present and non-empty

---

## Author + version

Generated by Module 2D at `2026-05-20` from:

- Niche: `pool-services`
- Playbook: `templates/pool-services/niche-playbook/`
